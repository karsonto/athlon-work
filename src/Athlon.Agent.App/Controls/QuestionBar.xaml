<UserControl x:Class="Athlon.Agent.App.Controls.QuestionBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:Athlon.Agent.App.Localization">
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsVisible}" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <Style x:Key="QuestionOptionToggleStyle" TargetType="ToggleButton">
            <Setter Property="Foreground" Value="{DynamicResource Brush.SubtleText}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,5" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.HoverNeutral}" />
                                <Setter Property="Foreground" Value="{DynamicResource Brush.Text}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.HoverNeutralAlt}" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Brush.ModePlanBorder}" />
                                <Setter Property="Foreground" Value="{DynamicResource Brush.Text}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource Brush.Composer}"
            BorderBrush="{DynamicResource Brush.ModePlanBorder}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="12,10"
            Margin="0,0,0,8">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{loc:Localize Key=AskUser_Title}"
                           FontWeight="SemiBold"
                           FontSize="12.5"
                           Foreground="{DynamicResource Brush.Text}"
                           VerticalAlignment="Center" />
                <Button Grid.Column="1"
                        HorizontalAlignment="Right"
                        Content="{loc:Localize Key=AskUser_Cancel}"
                        Command="{Binding CancelCommand}"
                        ToolTip="{loc:Localize Key=AskUser_Cancel}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource GhostButtonStyle}">
                            <Setter Property="Padding" Value="10,3" />
                            <Setter Property="FontSize" Value="11" />
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock Text="{Binding Prompt}"
                                       TextWrapping="Wrap"
                                       FontSize="12.5"
                                       Foreground="{DynamicResource Brush.Text}"
                                       Margin="0,0,0,6" />
                            <ItemsControl ItemsSource="{Binding Options}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ToggleButton Content="{Binding Label}"
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                      Margin="0,0,6,4"
                                                      Style="{StaticResource QuestionOptionToggleStyle}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Orientation="Vertical" Visibility="{Binding ShowFreeText, Converter={StaticResource BoolToVisConverter}}">
                <TextBlock Text="{loc:Localize Key=AskUser_NotesPlaceholder}"
                           FontSize="11"
                           Foreground="{DynamicResource Brush.SubtleText}"
                           Margin="0,0,0,4" />
                <TextBox Text="{Binding FreeText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MaxHeight="72"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="12" />
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,10,0,0">
                <Button Content="{loc:Localize Key=AskUser_Submit}"
                        Command="{Binding SubmitCommand}"
                        ToolTip="{loc:Localize Key=AskUser_Submit}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource GhostButtonStyle}">
                            <Setter Property="Padding" Value="18,6" />
                            <Setter Property="FontSize" Value="12" />
                            <Setter Property="Background" Value="{DynamicResource Brush.HoverNeutralAlt}" />
                            <Setter Property="Foreground" Value="{DynamicResource Brush.Text}" />
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
