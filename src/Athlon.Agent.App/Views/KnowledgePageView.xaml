<UserControl x:Class="Athlon.Agent.App.Views.KnowledgePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:app="clr-namespace:Athlon.Agent.App"
             xmlns:controls="clr-namespace:Athlon.Agent.App.Controls"
             xmlns:loc="clr-namespace:Athlon.Agent.App.Localization">
    <Grid Background="{DynamicResource Brush.ChatBackground}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Height="{x:Static app:AppLayoutMetrics.PanelHeaderHeight}"
                        Background="{DynamicResource Brush.Chrome}"
                        BorderBrush="{DynamicResource Brush.Border}"
                        BorderThickness="0,0,0,1"
                        Padding="24,0">
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Command="{Binding NavigateCommand}"
                                    CommandParameter="Chat"
                                    Style="{StaticResource GhostIconButtonStyle}"
                                    Margin="0,0,8,0"
                                    ToolTip="{loc:Localize Key=Common_BackToChat}"
                                    Content="←" />
                            <ContentControl Style="{StaticResource KnowledgeNavIconStyle}"
                                            Width="28"
                                            Height="28"
                                            Margin="0,0,10,0"
                                            VerticalAlignment="Center" />
                            <TextBlock Text="{loc:Localize Key=Knowledge_Title}"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="1" DataContext="{Binding KnowledgePageVm}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="380" MinWidth="280" MaxWidth="560" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Background="{DynamicResource Brush.Panel}"
                            BorderBrush="{DynamicResource Brush.Border}"
                            BorderThickness="0,0,1,0"
                            Padding="14">
                        <DockPanel LastChildFill="True">
                            <StackPanel DockPanel.Dock="Top">
                                <TextBlock Text="{loc:Localize Key=Knowledge_Modules}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,10" />

                                <!-- Empty state: no module selected -->
                                <Border x:Name="KnowledgeModuleEmptyState"
                                        BorderThickness="2"
                                        BorderBrush="{DynamicResource Brush.Border}"
                                        CornerRadius="10"
                                        Padding="16"
                                        MinHeight="80"
                                        Margin="0,0,0,14">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasModuleSelected}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{loc:Localize Key=Knowledge_NoModules}"
                                                   Foreground="{DynamicResource Brush.Text}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   TextWrapping="Wrap"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,0,0,6" />
                                        <TextBlock Text="{loc:Localize Key=Knowledge_NoModulesHint}"
                                                   Foreground="{DynamicResource Brush.SubtleText}"
                                                   FontSize="12"
                                                   TextWrapping="Wrap"
                                                   HorizontalAlignment="Center"
                                                   TextAlignment="Center"
                                                   Margin="0,0,0,12" />
                                        <Button Content="{loc:Localize Key=Knowledge_NewModule}"
                                                Command="{Binding AddModuleCommand}"
                                                Style="{StaticResource GhostButtonStyle}"
                                                HorizontalAlignment="Center"
                                                Padding="16,6" />
                                    </StackPanel>
                                </Border>

                                <!-- Module form (only visible when a module is selected) -->
                                <StackPanel x:Name="KnowledgeModuleForm">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasModuleSelected}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="{loc:Localize Key=Knowledge_Name}" Foreground="{DynamicResource Brush.SubtleText}" />
                                    <TextBox Text="{Binding ModuleName, UpdateSourceTrigger=PropertyChanged}"
                                             Margin="0,4,0,8" />
                                    <TextBlock Text="{loc:Localize Key=Knowledge_Description}" Foreground="{DynamicResource Brush.SubtleText}" />
                                    <TextBox Text="{Binding ModuleDescription, UpdateSourceTrigger=PropertyChanged}"
                                             AcceptsReturn="True"
                                             MinHeight="62"
                                             TextWrapping="Wrap"
                                             Margin="0,4,0,10" />
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                        <Button Content="{loc:Localize Key=Common_Add}"
                                                Command="{Binding AddModuleCommand}"
                                                Style="{StaticResource GhostButtonStyle}"
                                                Margin="0,0,8,0" />
                                        <Button Content="{loc:Localize Key=Common_Refresh}"
                                                Command="{Binding RefreshCommand}"
                                                Style="{StaticResource GhostButtonStyle}"
                                                Margin="0,0,8,0" />
                                        <Button Content="{loc:Localize Key=Common_Save}"
                                                Command="{Binding SaveSelectedModuleCommand}"
                                                Style="{StaticResource GhostButtonStyle}" />
                                    </StackPanel>
                                </StackPanel>

                                <TextBlock Text="{loc:Localize Key=Knowledge_Documents}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,8" />
                                <TextBlock Text="{loc:Localize Key=Knowledge_DropHint}"
                                           Foreground="{DynamicResource Brush.SubtleText}"
                                           TextWrapping="Wrap"
                                           FontSize="12"
                                           Margin="0,0,0,8" />
                                <StackPanel Margin="0,0,0,8">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <ProgressBar IsIndeterminate="True"
                                                 Height="4"
                                                 Margin="0,0,0,6" />
                                    <TextBlock Text="{loc:Localize Key=Knowledge_Loading}"
                                               Foreground="{DynamicResource Brush.SubtleText}"
                                               FontSize="12" />
                                </StackPanel>
                            </StackPanel>
                            <controls:KnowledgeDocumentsDropZone />
                        </DockPanel>
                    </Border>

                    <GridSplitter Grid.Column="0"
                                  Style="{StaticResource LayoutColumnSplitterStyle}"
                                  ResizeDirection="Columns"
                                  ResizeBehavior="CurrentAndNext"
                                  Panel.ZIndex="2" />

                    <ScrollViewer Grid.Column="1"
                                  VerticalScrollBarVisibility="Auto"
                                  Padding="22">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding StatusText}"
                                           Foreground="{DynamicResource Brush.SubtleText}"
                                           TextWrapping="Wrap"
                                           VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="{loc:Localize Key=Knowledge_Reindex}"
                                            Command="{Binding ReindexSelectedDocumentCommand}"
                                            Style="{StaticResource GhostButtonStyle}" />
                                </StackPanel>
                            </Grid>

                            <StackPanel Margin="0,0,0,12">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsIndexing}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <ProgressBar Minimum="0"
                                             Maximum="100"
                                             Height="8"
                                             Value="{Binding IndexingProgress}"
                                             Margin="0,0,0,6" />
                                <TextBlock Text="{Binding IndexingProgressText}"
                                           Foreground="{DynamicResource Brush.SubtleText}"
                                           TextWrapping="Wrap"
                                           FontSize="12" />
                            </StackPanel>

                            <!-- Document Preview Card -->
                            <Border Style="{StaticResource SettingsCardStyle}"
                                    Padding="16">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="📄"
                                                   FontSize="16"
                                                   Margin="0,0,8,0"
                                                   VerticalAlignment="Center" />
                                        <TextBlock Text="{loc:Localize Key=Knowledge_DocumentPreview}"
                                                   FontSize="15"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                    <Border Background="{DynamicResource Brush.Chrome}"
                                            CornerRadius="8"
                                            Padding="14"
                                            MaxHeight="320"
                                            MinHeight="80">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                      HorizontalScrollBarVisibility="Disabled">
                                            <TextBlock Text="{Binding DocumentPreview}"
                                                       Foreground="{DynamicResource Brush.Text}"
                                                       TextWrapping="Wrap"
                                                       FontSize="13"
                                                       FontFamily="Consolas"
                                                       LineHeight="20" />
                                        </ScrollViewer>
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- Search Test Card -->
                            <Border Style="{StaticResource SettingsCardStyle}">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="🔍"
                                                   FontSize="15"
                                                   Margin="0,0,8,0"
                                                   VerticalAlignment="Center" />
                                        <TextBlock Text="{loc:Localize Key=Knowledge_SearchTest}"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                    <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                                             AcceptsReturn="True"
                                             MinHeight="72"
                                             TextWrapping="Wrap"
                                             Margin="0,0,0,10" />
                                    <Button Content="{loc:Localize Key=Knowledge_TestSearch}"
                                            Command="{Binding TestSearchCommand}"
                                            Style="{StaticResource GhostButtonStyle}"
                                            HorizontalAlignment="Left"
                                            Margin="0,0,0,10" />
                                    <Border Background="{DynamicResource Brush.Chrome}"
                                            CornerRadius="8"
                                            Padding="14"
                                            MinHeight="120"
                                            MaxHeight="340">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                      HorizontalScrollBarVisibility="Disabled">
                                            <TextBlock Text="{Binding SearchResults}"
                                                       Foreground="{DynamicResource Brush.Text}"
                                                       TextWrapping="Wrap"
                                                       FontSize="13"
                                                       LineHeight="20" />
                                        </ScrollViewer>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
    </Grid>
</UserControl>
