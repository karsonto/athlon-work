<Window x:Class="Athlon.Agent.App.Windows.MermaidPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Mermaid 图表预览"
        Width="1200"
        Height="820"
        MinWidth="720"
        MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource PreviewWindowStyle}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Mermaid 图表预览"
                           Style="{StaticResource PreviewWindowTitleStyle}" />
                <TextBlock Text="使用安装包内置 Mermaid 离线渲染，无需互联网"
                           Style="{StaticResource PreviewWindowSubtitleStyle}" />
            </StackPanel>
            <Button Content="×"
                    Width="40"
                    Height="40"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Click="CloseButton_OnClick"
                    ToolTip="关闭"
                    Style="{StaticResource GhostButtonStyle}" />
        </Grid>

        <Border Grid.Row="1"
                Style="{StaticResource PreviewContentFrameStyle}">
            <wv2:WebView2 x:Name="PreviewWebView" />
        </Border>
    </Grid>
</Window>
